const setupTheme = () => {
    LiveLike.applyTheme({
        widgets: {
            prediction: {
                header: {
                    background: { format: 'fill', color: '#FF0000' }
                }
            },
            poll: {
                header: {
                    background: { format: 'fill', color: '#FF0000' }
                }
            },
            quiz: {
                header: {
                    background: { format: 'fill', color: '#FF0000' }
                }
            }
        }
    });
};